import styled from "styled-components";

const FilterStyled = styled.form`
  font-family: "Roboto";
  padding: 20px;

  .title {
    padding: 10px 0;
  }

  .input-text {
    padding: 10px;
    font-size: 1.3rem;
  }

  input {
    font-family: inherit;
  }

  .label-input {
    padding: 10px;
    font-family: inherit;
  }
`;

export default FilterStyled;
